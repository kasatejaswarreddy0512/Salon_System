import { createTheme } from "@mui/material";

const greenTheme = createTheme({
    palette: {
        mode: "light",
        primary: {
            main: "#019031",
        },
        secondary: {
            main: "#eaf0f1",
        },
    },
});

export default greenTheme;
